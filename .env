VITE_API_URL=https://backend-orpin-nu-20.vercel.app
# In development mode, you don't need to set this since Vite will proxy requests
# If your API runs on a different port, update this and the proxy configuration in vite.config.ts
DEV=true